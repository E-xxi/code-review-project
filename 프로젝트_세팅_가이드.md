# 코테 코드 분석기 — 프로젝트 세팅 가이드 (macOS 기준)
코드를 입력하면 클로드가 이를 분석해서 , 코드의 부족한 부분, 효율화 부분, 부족한 개념등을 반환해 웹에 보여주는 프로젝트 + 히스토리도 존재

## 0. 전체 그림 먼저

```
[React (Vite) + Monaco Editor]  ← 프론트엔드, 사용자가 Python 코드 붙여넣는 화면
        ↓ HTTP 요청
[Spring Boot (Java)]            ← 백엔드, 분석 로직 + DB 연동
   ├─ Claude API 호출 (코드 분석)
   └─ PostgreSQL 저장/조회
        ↓
[PostgreSQL]                    ← 분석 히스토리 저장
```

프로젝트 폴더는 이렇게 두 개로 나눠서 만들 거예요.

```
code-review-project/
├── backend/   (Spring Boot)
└── frontend/  (React)
```

처음이니까 **1단계(DB 없이 분석 기능만 동작)** 까지 완주하는 걸 목표로 잡고, 그 이후 단계는 맨 아래 로드맵에 정리해둘게요.

---

## 1. 필수 도구 설치

### 1-1. Homebrew (맥 패키지 관리자, 이미 있으면 스킵)

터미널(Terminal.app) 열고:

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

설치 끝나면 안내 문구에 따라 PATH 등록 (`brew` 명령어가 어디서든 되는지 확인):

```bash
brew --version
```

### 1-2. Git

맥에는 기본 내장되어 있는 경우가 많아요. 확인:

```bash
git --version
```

없으면:

```bash
brew install git
```

GitHub 계정이 없으면 [github.com](https://github.com)에서 미리 하나 만들어두세요. 코드 백업 + 나중에 배포할 때 반드시 필요합니다.

### 1-3. Java (JDK 21)

Spring Boot는 최신 버전 기준 Java 17 이상을 요구해요. 21(LTS)로 설치 추천.

```bash
brew install openjdk@21
```

설치 후 안내되는 심볼릭 링크 명령어도 그대로 실행해주세요 (brew가 알려줌, 보통 아래와 비슷함):

```bash
sudo ln -sfn /opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk /Library/Java/JavaVirtualMachines/openjdk-21.jdk
```

확인:

```bash
java -version
```

### 1-4. IntelliJ IDEA (Java용 IDE)

Java 프로젝트는 VSCode보다 IntelliJ가 훨씬 편해요 (자동완성, 에러 잡기, Spring 통합).

```bash
brew install --cask intellij-idea-ce
```

(Community Edition 무료 버전이에요. Ultimate은 유료지만 학생이면 무료 라이선스 신청 가능 — 나중에 필요하면 찾아보세요.)

### 1-5. Node.js (프론트엔드용)

버전 관리가 편한 `nvm` 추천:

```bash
brew install nvm
mkdir ~/.nvm
```

`~/.zshrc`에 아래 추가 (터미널에서 `open ~/.zshrc` 로 열어서 맨 아래 붙여넣기):

```bash
export NVM_DIR="$HOME/.nvm"
[ -s "/opt/homebrew/opt/nvm/nvm.sh" ] && \. "/opt/homebrew/opt/nvm/nvm.sh"
```

터미널 재시작 후:

```bash
nvm install --lts
nvm use --lts
node -v
npm -v
```

### 1-6. PostgreSQL

```bash
brew install postgresql@16
brew services start postgresql@16
```

설치 확인:

```bash
psql --version
```

DB 관리 GUI 툴도 하나 있으면 편해요 (테이블 보기, 데이터 확인):

```bash
brew install --cask dbeaver-community
```

### 1-7. VSCode (프론트엔드 작업용, 선택이지만 추천)

```bash
brew install --cask visual-studio-code
```

→ **정리:** Java/Spring 작업은 IntelliJ, React 작업은 VSCode 로 나눠서 쓰는 걸 추천해요.

---

## 2. 백엔드 프로젝트 생성 (Spring Boot)

### 2-1. Spring Initializr로 뼈대 생성

브라우저에서 [start.spring.io](https://start.spring.io) 접속 후 아래처럼 설정:

- **Project**: Maven
- **Language**: Java
- **Spring Boot**: 최신 안정 버전 (3.x)
- **Group**: `com.example`
- **Artifact**: `codereview`
- **Java**: 21
- **Dependencies** (오른쪽에서 검색해서 추가):
  - Spring Web
  - Spring Data JPA
  - PostgreSQL Driver
  - Lombok
  - Validation

`GENERATE` 클릭 → zip 다운로드 → 압축 풀어서 `code-review-project/backend` 로 이동/이름 변경.

### 2-2. IntelliJ에서 열기

IntelliJ 실행 → `Open` → `backend` 폴더 선택 → Maven이 자동으로 의존성 다운로드할 때까지 대기 (오른쪽 아래 진행바).

### 2-3. 첫 실행 확인

`src/main/java/com/example/codereview/CodereviewApplication.java` 파일 열고 실행 버튼(▶) 클릭.

터미널에 `Tomcat started on port 8080` 같은 로그가 뜨면 성공. 브라우저에서 `http://localhost:8080` 접속했을 때 에러 페이지(Whitelabel Error Page)가 떠도 정상이에요 — 아직 아무 API도 안 만들었기 때문입니다.

---

## 3. 프론트엔드 프로젝트 생성 (React + Vite)

터미널에서:

```bash
cd code-review-project
npm create vite@latest frontend -- --template react
cd frontend
npm install
```

Monaco Editor(코드 입력창) 설치:

```bash
npm install @monaco-editor/react
```

실행 확인:

```bash
npm run dev
```

터미널에 뜨는 주소(보통 `http://localhost:5173`)를 브라우저에서 열어서 Vite 기본 화면이 보이면 성공.

---

## 4. Anthropic API 키 발급

1. [console.anthropic.com](https://console.anthropic.com) 접속, 가입/로그인
2. API Keys 메뉴에서 새 키 발급
3. 키는 **절대 코드에 직접 쓰지 말고** 환경변수로 관리

Spring Boot에서는 `backend/src/main/resources/application.properties`에 직접 쓰지 말고, 아래처럼 환경변수 참조 방식을 씁니다:

```properties
anthropic.api.key=${ANTHROPIC_API_KEY}
```

그리고 로컬 실행 시 터미널에서:

```bash
export ANTHROPIC_API_KEY="여기에_발급받은_키"
```

(또는 IntelliJ의 Run Configuration → Environment Variables 에 등록해두면 매번 export 안 해도 돼요.)

---

## 5. Git / GitHub 세팅

프로젝트 루트(`code-review-project`)에서:

```bash
git init
```

`.gitignore` 파일 생성 (매우 중요 — API 키, 빌드 파일이 커밋되는 걸 방지):

```
# backend
backend/target/
backend/.idea/
backend/*.iml

# frontend
frontend/node_modules/
frontend/dist/

# 공통
.env
.DS_Store
```

GitHub에서 새 저장소(예: `code-review-project`) 만든 후:

```bash
git add .
git commit -m "init: project structure"
git branch -M main
git remote add origin <저장소_URL>
git push -u origin main
```

---

## 6. 1단계 MVP 구현 순서 (DB 없이, 분석 기능만)

지금까지가 세팅이고, 여기서부터가 실제 개발 순서예요. 이 순서대로 하나씩 완성하면서 진행하는 걸 추천합니다.

1. **백엔드: DTO 정의** — 요청(`code: String`)과 응답(복잡도, 개선점, 부족한 개념 리스트) 형태를 클래스로 먼저 설계
2. **백엔드: Claude API 호출 서비스 작성** — `WebClient`로 Anthropic API에 요청 보내고, 구조화된 JSON 응답을 파싱하는 로직
3. **백엔드: Controller 작성** — `POST /api/analyze` 엔드포인트, 프론트에서 코드 받아서 서비스 호출 후 결과 반환
4. **백엔드: CORS 설정** — 프론트(5173)에서 백엔드(8080)로 요청 가능하도록 허용 설정 필수
5. **프론트: 코드 입력 화면** — Monaco Editor + "분석하기" 버튼
6. **프론트: API 연동** — `fetch`/`axios`로 백엔드 호출, 로딩 상태 처리
7. **프론트: 결과 화면** — 응답받은 JSON을 카드 형태로 렌더링 (복잡도 / 개선점 / 부족한 개념)
8. **로컬 end-to-end 테스트** — 실제 코테 코드 붙여넣고 처음부터 끝까지 흐름 확인

이 8단계가 끝나면 로컬에서 동작하는 완전한 MVP가 만들어집니다. 배포는 이게 끝난 뒤에 진행하는 게 안전해요 (동작 확인 → 배포 순서).

---

## 7. 이후 로드맵 (요약)

| 단계 | 내용 |
|---|---|
| 2단계 | PostgreSQL 연동 (JPA Entity, Repository) — 분석 결과 저장/조회 |
| 3단계 | ANTLR4로 Python AST 파싱 → 구조적 특징 추출해서 프롬프트 컨텍스트로 활용 |
| 4단계 | 취약 개념 통계 집계 API + 프론트 대시보드(차트) |
| 5단계 | 배포 (Frontend → Vercel, Backend+DB → Railway 또는 Render) |

---

## 막히면 확인할 것 (자주 발생하는 문제)

- **`brew services start postgresql@16` 했는데 접속 안 됨** → `psql postgres` 로 접속 테스트, 안 되면 `brew services restart postgresql@16`
- **IntelliJ에서 Maven 의존성이 계속 안 잡힘** → 오른쪽 Maven 탭 → 새로고침(원형 화살표) 아이콘
- **프론트에서 백엔드 호출 시 CORS 에러** → Spring Boot `@CrossOrigin` 또는 `WebMvcConfigurer` 설정 누락 확인
- **`ANTHROPIC_API_KEY` 인식 안 됨** → 터미널을 새로 열었는지, IntelliJ Run Configuration에 환경변수가 제대로 들어갔는지 확인

---

막히는 단계 생기면 그 부분만 콕 집어서 다시 물어보셔도 되고, 지금 바로 2단계(백엔드 DTO + Claude API 호출 서비스 코드)부터 같이 짜봐도 됩니다.
