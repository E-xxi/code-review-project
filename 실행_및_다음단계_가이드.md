# 실행 방법 & 앞으로 해야 할 일

`프로젝트_세팅_가이드.md`의 세팅 단계(1~5)와, 1단계 MVP 코드(DTO/Claude 호출 서비스/Controller/CORS/프론트엔드 화면)까지 이미 만들어진 상태입니다. 여기서는 (1) 지금 당장 실행하는 방법, (2) Claude API 키를 어디에 넣어야 하는지, (3) 다음에 직접 해야 할 일을 정리합니다.

---

## 1. 실행 방법

### 새 터미널을 열 때마다 확인할 것

nvm/Java는 `.zshrc`에 등록되어 있어서 새 터미널을 열면 자동으로 잡힙니다.

```bash
node -v      # v24.x
java -version  # openjdk 21.x
```

### 백엔드 실행 (Spring Boot)

```bash
export ANTHROPIC_API_KEY="발급받은_키"   # 2번 항목 참고
cd backend
./mvnw spring-boot:run
```

- 로그에 `Tomcat started on port 8080`, `Started CodereviewApplication`이 뜨면 성공
- `ANTHROPIC_API_KEY`를 안 넣으면 `Could not resolve placeholder 'ANTHROPIC_API_KEY'` 에러로 아예 기동이 안 됩니다 (의도된 동작)

### 프론트엔드 실행 (React + Vite)

```bash
cd frontend
npm run dev
```

- 터미널에 뜨는 `http://localhost:5173`을 브라우저에서 열기
- CORS는 이미 백엔드에 설정되어 있어서 (`http://localhost:5173` 허용) 프론트에서 바로 분석 요청이 가능합니다

### 둘 다 띄우기

터미널 탭/창을 2개 열어서 backend, frontend를 각각 실행하면 됩니다. 둘 다 뜬 상태에서 브라우저로 `http://localhost:5173` 접속 → 코드 입력 → "분석하기" 클릭까지가 전체 흐름입니다.

---

## 2. Claude API 키, 어디에 붙여야 하나

### 2-1. 키 발급

1. [console.anthropic.com](https://console.anthropic.com) 접속, 가입/로그인
2. API Keys 메뉴에서 새 키 발급 (`sk-ant-...` 형태)

### 2-2. 키를 넣는 곳 (코드에는 절대 직접 쓰지 않음)

키가 실제로 쓰이는 경로는 이렇습니다:

```
환경변수 ANTHROPIC_API_KEY
  → backend/src/main/resources/application.properties 의 anthropic.api.key=${ANTHROPIC_API_KEY}
  → backend/.../service/ClaudeAnalysisService.java 생성자의 @Value("${anthropic.api.key}") apiKey
  → WebClient 요청 헤더 x-api-key
```

즉, **코드나 `application.properties`를 직접 수정할 필요는 없고, 환경변수만 채워주면 됩니다.** 방법은 둘 중 하나:

**방법 A. 터미널에서 export (가장 간단, 터미널 껐다 켜면 다시 해야 함)**

```bash
export ANTHROPIC_API_KEY="sk-ant-여기에_발급받은_키"
cd backend
./mvnw spring-boot:run
```

**방법 B. IntelliJ Run Configuration에 등록 (한 번만 설정하면 계속 유지됨)**

1. IntelliJ에서 `backend` 프로젝트 열기
2. 상단 `CodereviewApplication` 실행 설정 옆 드롭다운 → `Edit Configurations...`
3. `Environment variables` 항목에 `ANTHROPIC_API_KEY=sk-ant-여기에_발급받은_키` 추가
4. 이후로는 IntelliJ에서 ▶ 실행할 때마다 자동으로 적용됨

두 방법 중 하나만 하면 됩니다. 지금까지 저는 실제 키 없이 더미 값과 모킹된 응답으로만 배선(연결)이 맞는지 확인했고, 진짜 Claude 응답으로 테스트한 적은 없습니다.

---

## 3. 지금까지 완료된 것

- [x] DTO: `dto/AnalyzeRequest.java` (`problem`, `code`), `dto/AnalyzeResponse.java` (복잡도/스타일피드백/패턴/부족개념/개선안)
- [x] `service/ClaudeAnalysisService.java` — Anthropic Messages API에 tool-use 방식으로 요청, 구조화된 JSON을 `AnalyzeResponse`로 변환
- [x] `config/WebClientConfig.java` — Anthropic 호출용 `WebClient` 빈
- [x] `controller/AnalyzeController.java` — `POST /api/analyze`
- [x] `config/CorsConfig.java` — `localhost:5173` 허용
- [x] 프론트엔드: `card-style-export.html` 목업 스타일 그대로 구현 (Header, 문제/코드 입력 패널 + Monaco Editor, 결과 카드 4종, 빈 상태/에러 상태)
- [x] 프론트-백엔드 연동 코드 (`api/analyzeCode.js`), Playwright로 화면 렌더링/에러 처리까지 확인 완료
- [x] **실제 `ANTHROPIC_API_KEY`로 진짜 end-to-end 테스트 완료** — Two Sum 브루트포스 코드를 넣었을 때 `AnalyzeResponse` 스키마에 정확히 맞는 고품질 응답을 받았고, 브라우저에서 카드 렌더링까지 확인함
- [x] **ANTLR4 Python 파싱 (3단계)** — `grammars-v4`의 공식 Python3 문법을 가져와 `backend/src/main/antlr4/`에 배치하고, 구조적 특징(반복문 개수/최대 중첩 깊이, 조건 분기 개수, 함수/재귀 여부, 딕셔너리·셋 사용, 정렬 함수 사용)을 뽑아내는 `analysis/PythonStructureAnalyzer.java`를 작성해서 `ClaudeAnalysisService`의 프롬프트에 컨텍스트로 덧붙임. 유닛 테스트 5개로 검증 완료

아직 안 한 것:

- [ ] PostgreSQL 연동 (2단계, 아래 로드맵 참고)
- [ ] ANTLR4 구조 분석이 실제 Claude 응답 품질을 얼마나 개선하는지는 아직 실측 못함 (API 키가 무효화돼서 재확인 필요 — 아래 참고)

`backend/src/main/resources/application.properties`에는 아직 이런 줄이 남아 있습니다:

```properties
spring.autoconfigure.exclude=org.springframework.boot.jdbc.autoconfigure.DataSourceAutoConfiguration,org.springframework.boot.hibernate.autoconfigure.HibernateJpaAutoConfiguration,org.springframework.boot.data.jpa.autoconfigure.DataJpaRepositoriesAutoConfiguration
```

PostgreSQL 없이도 서버가 뜨게 하려고 DB 자동 설정을 꺼둔 것이라, **2단계(PostgreSQL 연동)를 시작할 때 이 줄을 지우고** `spring.datasource.*`를 채워야 합니다.

---

## 4. 다음에 직접 해야 할 일

1. **Claude API 키 재발급** — 이전에 테스트에 쓰던 키가 지금 무효화된 상태입니다(`API key is invalid.`, 유출 위험 때문에 폐기된 것으로 보임). [console.anthropic.com](https://console.anthropic.com)에서 새로 발급받아 등록해야 다시 실제 분석이 동작합니다
2. **새 키로 ANTLR4 구조 분석 포함 end-to-end 재확인**
   - 백엔드/프론트엔드 둘 다 실행
   - 브라우저에서 코드 붙여넣고 "분석하기" 클릭
   - ANTLR4가 뽑아낸 구조 정보(반복문 중첩 깊이, 재귀 여부 등)가 프롬프트에 실제로 잘 반영돼서 응답 품질에 도움이 되는지 확인
   - 만약 Claude가 `AnalyzeResponse` 스키마와 안 맞는 응답을 주는 경우가 있으면 `ClaudeAnalysisService`의 프롬프트나 tool schema 설명(`description`)을 조금 더 구체적으로 다듬어야 할 수 있음
3. **(선택) 언어 선택 연동** — 지금 프론트 헤더의 언어 드롭다운(Python/Java/C++/JavaScript)은 Monaco 에디터 문법 강조에만 쓰이고 있고, 백엔드에는 전달되지 않습니다. 참고로 지금 ANTLR4 구조 분석은 **Python 코드만** 지원합니다 (Java/C++ 코드를 넣으면 파싱이 조용히 실패하고 구조 분석 없이 진행됨). Claude에게 언어 정보를 명시적으로 주고 싶다면 `AnalyzeRequest`에 `language` 필드를 추가하고 프롬프트에 포함시키면 됩니다
4. **2단계로 넘어가기: PostgreSQL 연동**
   - `application.properties`의 `spring.autoconfigure.exclude` 줄 제거
   - JPA Entity(분석 기록), Repository 작성
   - Controller에서 분석 결과를 저장/조회하도록 확장

---

## 5. 그다음 로드맵 (참고용, 지금 당장은 아님)

| 단계 | 내용 |
|---|---|
| 2단계 | PostgreSQL 연동 — JPA Entity/Repository 작성, 분석 결과 저장/조회 |
| ~~3단계~~ | ~~ANTLR4로 Python AST 파싱~~ → 완료. `analysis/PythonStructureAnalyzer.java` 참고 |
| 4단계 | 취약 개념 통계 집계 API + 프론트 대시보드(차트) |
| 5단계 | 배포 (Frontend → Vercel, Backend+DB → Railway 또는 Render) |
