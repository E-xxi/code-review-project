# 실행 방법 & 앞으로 해야 할 일

## 1. 실행 방법

### 새 터미널을 열 때마다 확인할 것

```bash
node -v      # v24.x
java -version  # openjdk 21.x
```

### 백엔드 실행 (Spring Boot)

```bash
export ANTHROPIC_API_KEY="발급받은_키"   # 2번 항목 참고
cd backend
./mvnw spring-boot:run
```

- 로그에 `Tomcat started on port 8080`, `Started CodereviewApplication`이 뜨면 성공
- `ANTHROPIC_API_KEY`를 안 넣으면 `Could not resolve placeholder 'ANTHROPIC_API_KEY'` 에러

### 프론트엔드 실행 (React + Vite)

```bash
cd frontend
npm run dev
```

- 터미널에 뜨는 `http://localhost:5173`을 브라우저에서 열기
